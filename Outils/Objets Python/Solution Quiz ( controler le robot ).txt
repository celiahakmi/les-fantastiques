1- RPi.GPIO (seulement pour les moteurs)

2- On utilise PyGame pour le clavier et RPi.GPIO pour les moteur

3- PyGame sait afficher à l'écran, RPi.GPIO sait contrôler les broches électriques 

4- if pygame.key.get_pressed()[pygame.K_z]

5- Il faut : importer pygame + l'initialiser + avoir une boucle qui tourne.


Schéma à retenir :
 Clavier(tu appuies sur une touche) -> PyGame(détecte la touche) -> RPi.GPIO(reçoit l'instruction) -> Moteurs(actionnent les roues)
